import React from 'react';
import { Budget } from '../../interfaces/Budget';

interface Props {
  budget: Budget;
}

const BudgetDetails = ({ budget }: Props) => {
  return <div>BudgetDetails</div>;
};

export default BudgetDetails;
